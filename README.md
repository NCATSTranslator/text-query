# text-query

A FastMCP server.

## Setup

Install dependencies using uv:

```bash
uv sync
```

## Development

Run the server in development mode:

```bash
uv run src/server.py
```

## Usage

Add to your MCP settings configuration to use this server with Claude Desktop or other MCP clients.
